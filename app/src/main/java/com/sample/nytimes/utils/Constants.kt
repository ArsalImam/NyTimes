package com.sample.nytimes.utils

import org.apache.commons.lang.math.NumberUtils

/**
 * [author] by `Arsal Imam`
 * [created] on 5/18/2020
 */
object Constants {
    val NUMBER_TWO: Int = NumberUtils.INTEGER_ONE + NumberUtils.INTEGER_ONE
    val NUMBER_THREE: Int = NUMBER_TWO + NumberUtils.INTEGER_ONE
}